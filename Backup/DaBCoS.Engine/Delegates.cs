using System;

namespace DaBCoS.Engine
{
	/// <summary>
	/// Delegate that points to the logging function 
	/// if the user has one
	/// </summary>
	public delegate void LogFunction(string message);
}
